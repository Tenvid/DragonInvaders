using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LimitMovements : MonoBehaviour
{
    //Stores bullets which reach the top of screen
    //public static void LimitTopBullets(Player player, GameObject topLimit, List<Bullet> storedBullets, List<Bullet> shotBullets)
    //{
    //    for(int i = 0; i < shotBullets.Count; i++)
    //    {
    //        if (topLimit.GetComponent<Collider2D>().IsTouching(shotBullets[i].GetComponent<Collider2D>()))
    //        {
    //            storedBullets.Add(shotBullets[i]);
    //            shotBullets.RemoveAt(i);
    //            player.ActualShoot--;
    //        }
    //    }
    //}
}
